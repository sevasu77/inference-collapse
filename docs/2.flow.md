[UI (Streamlit / Canvas)]
        │
        │ ① ユーザー操作 / 証拠入力
        ▼
POST /inference
        │
        ▼
[LLM Service (Gemma / GPT)]
        │
        │ ② 推論・分析・確信度計算
        ▼
┌──────────────────────────────┐
│ inference result              │
│ ─ report (推論結果)          │
│ ─ confidence (確信度)        │
│ ─ severity (危険度)          │
│ ─ contradiction (矛盾フラグ)  │
└──────────────────────────────┘
        │
        ▼
POST /state/update
        │
        ▼
[State Manager]
        │
        │ ③ 世界のセーブデータ更新
        ▼
┌──────────────────────────────┐
│ WorldState / AIState / EngineState │
└──────────────────────────────┘
        │
        ▼
POST /simulate
        │
        ▼
[Game Engine]
        │
        │ ④ 物理世界の再計算
        │    - enemy speed
        │    - FOV
        │    - glitch intensity
        ▼
[Render / Canvas Frontend]
        │
        │ ⑤ 画面反映
        ▼
🎨 プレイヤー体験として出力
