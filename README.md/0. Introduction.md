# Introduction

## Welcome

**Inference Collapse** は、LLM の推論結果を単なるテキスト出力として扱うのではなく、**ゲーム世界を変化させる物理パラメータ**として利用する実験的なシステムです。

一般的な AI アプリケーションでは、

```
LLM → Text Response → User
```

という流れになります。

一方、本プロジェクトでは、

```
LLM → Cognitive State → Physics → Simulation → Player Experience
```

という新しいパイプラインを採用しています。

AI は文章を生成するだけではなく、世界の状態を変化させる認知コンポーネントとして機能します。

---

# Project Goal

本プロジェクトの目的は、ゲームを作ることではありません。

目指しているのは、

**「AI の認知状態がどのようにゲーム世界へ影響を与えるのか」を観測・実験できるシステムを構築すること**

です。

そのため、LLM の推論結果はゲーム難易度へ直接適用されるのではなく、物理パラメータへ変換されます。

例えば、

| AI Output     | World Effect          |
| ------------- | --------------------- |
| Confidence    | Enemy Speed           |
| Severity      | Spatial Distortion    |
| Contradiction | Perception Distortion |

このように、**認知状態を物理法則へ変換する**ことが、本システムの中核となるアイデアです。

---

# Design Philosophy

本プロジェクトは、以下の設計原則に基づいています。

## Prototype First

まず動作するプロトタイプを構築し、アイデアの実現可能性を検証する。

## Post-Hoc Architecture

動作するプロトタイプから設計を抽出し、責務を整理してアーキテクチャへ発展させる。

## Separation of Responsibilities

AI・状態管理・シミュレーション・UI を独立したコンポーネントとして設計し、それぞれが明確な責務を持つ構造を目指す。

## Inference-to-Physics Mapping

LLM の推論結果をそのままゲームへ反映するのではなく、物理法則へ変換することで、AI とゲームロジックを疎結合に保つ。

---

# Repository Guide

このリポジトリには、プロジェクトの設計から実装までを段階的に説明するドキュメントが含まれています。

| Document                    | Purpose                       |
| --------------------------- | ----------------------------- |
| `README.md`                 | プロジェクト概要                      |
| `architecture.md`           | システム全体の設計                     |
| `flow.md`                   | データフローと処理シーケンス                |
| `live-system.md`            | 現在実装されている構成                   |
| `problem-statement.md`      | プロトタイプが抱えていた構造的課題             |
| `architecture-evolution.md` | 設計がどのように進化したか                 |
| `final-architecture.md`     | 再設計後の目標アーキテクチャ                |
| `key-design-decisions.md`   | 主要な設計判断とその理由                  |
| `future-work.md`            | 今後の拡張計画                       |
| `docs/adr/`                 | Architecture Decision Records |

---

# Reading Order

初めて読む場合は、以下の順番を推奨します。

```text
README
    ↓
Introduction
    ↓
Architecture
    ↓
Flow
    ↓
Live System
    ↓
Problem Statement
    ↓
Architecture Evolution
    ↓
Final Architecture
    ↓
Key Design Decisions
    ↓
ADR
    ↓
Future Work
```

この順序で読むことで、

* プロジェクトの目的
* 現在の実装
* 設計上の課題
* 再設計の理由
* 最終的なアーキテクチャ

までを、一連の流れとして理解できます。

---

# About This Repository

このリポジトリは、完成したソフトウェアを公開することだけを目的としていません。

**「プロトタイプからアーキテクチャへ進化していく過程」**そのものを記録することも、プロジェクトの重要な目的の一つです。

そのため、各ドキュメントでは、

* なぜこの設計を選択したのか
* どのような問題が発生したのか
* どのように構造を改善したのか

を、設計判断とともに記録しています。

---

# Vision

Inference Collapse は、ゲームエンジンでも AI ライブラリでもありません。

**AI の認知状態を物理世界へ変換し、その振る舞いを観測するための実験プラットフォーム**です。

本プロジェクトを通して、AI・ゲーム・シミュレーションを組み合わせた新しいアーキテクチャの可能性を探求していきます。
